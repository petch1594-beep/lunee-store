<?php
require_once __DIR__.'/config.php'; if (current_user()) redirect('index.php');
if ($_SERVER['REQUEST_METHOD'] === 'POST') { $email = strtolower(trim($_POST['email'] ?? '')); $s = db()->prepare('SELECT * FROM users WHERE email=?'); $s->execute([$email]); $u = $s->fetch(); if (!$u || !password_verify($_POST['password'] ?? '', $u['password_hash'])) flash('error', 'อีเมลหรือรหัสผ่านไม่ถูกต้อง'); else { if (!$u['is_verified']) db()->prepare('UPDATE users SET is_verified=1,verification_code=NULL,code_expires_at=NULL WHERE id=?')->execute([$u['id']]); $_SESSION['user'] = ['id'=>$u['id'],'name'=>$u['name'],'email'=>$u['email'],'role'=>$u['role']]; flash('success', 'ยินดีต้อนรับกลับมา'); redirect('index.php'); } }
$page_title = 'เข้าสู่ระบบ | '.STORE_NAME; require __DIR__.'/partials_header.php';
?>
<main class="auth-wrap auth-experience"><aside class="auth-showcase"><div class="showcase-copy"><div class="eyebrow">WELCOME BACK</div><h2>กลับมาหา<br>สิ่งที่สบาย<br>สำหรับคุณ</h2><p>ทุกชิ้นที่เลือกไว้ และทุกสไตล์ที่ชอบ ยังรอคุณอยู่ตรงนี้</p></div><div class="showcase-note"><span>MEMBER ACCESS</span><b>Simple looks.<br>Better days.</b></div></aside><section class="auth-card auth-card-wide"><div class="auth-heading"><div class="auth-kicker"><span class="step-badge">↗</span> MEMBER SIGN IN</div><h1>ยินดีต้อนรับกลับมา</h1><p>เข้าสู่ระบบเพื่อจัดการบัญชีและทุกการเลือกของคุณ</p></div><form method="post" class="form-stack"><div class="field"><label for="email">อีเมล</label><div class="input-wrap"><span>✉</span><input id="email" name="email" type="email" autocomplete="email" placeholder="you@email.com" required autofocus></div></div><div class="field"><div class="label-row"><label for="password">รหัสผ่าน</label><a href="#" class="forgot-link" onclick="alert('ระบบตั้งรหัสผ่านใหม่จะเพิ่มในขั้นถัดไป');return false">ลืมรหัสผ่าน?</a></div><div class="input-wrap password-wrap"><span>⌁</span><input id="password" name="password" type="password" autocomplete="current-password" placeholder="กรอกรหัสผ่านของคุณ" required><button class="password-toggle" type="button" aria-label="แสดงรหัสผ่าน">แสดง</button></div></div><button class="button full submit-button"><span>เข้าสู่ระบบ</span><b>→</b></button></form><div class="divider"><span>หรือเข้าสู่ระบบด้วย</span></div><div class="social"><a href="auth/google_start.php"><b class="google-mark">G</b> Google</a><a href="#" onclick="alert('Facebook OAuth ต้องตั้งค่า App ID ก่อน');return false"><b class="facebook-mark">f</b> Facebook</a></div><p class="fine">ยังไม่มีบัญชี? <a href="register.php">สมัครสมาชิก</a></p></section></main><?php require __DIR__.'/partials_footer.php'; ?>
